type User {
  id: ID!
  name: String
  email: String
  token: String # Todo
  access: AccessGroup # AccessGroup
}

type Query {
  users: [User!]!
}

type Mutation {
  addUser(
    name: String!
    email: String!
    token: String!
    access: ID!
  ): User
}
